image: alpine/edge
artifacts:
  - feef/dist.zip
packages:
  - go
  - zip
sources:
  - https://git.sr.ht/~skuzzymiglet/feef
tasks:
  - build: |
      cd feef
      ./gogo-release
      zip -r dist.zip dist
